<%= form_for @card do |f| %>
	<% if @card.errors.any? %>
	<div id ="error_explanation">
		<h2><%= pluralize(@card.errors.count, "error") %> prohibited
			this card from being saved: </h2>
		<ul>
			<% @card.errors.full_messages.each do |msg| %>
				<li><%= msg %></li>
			<% end %>
		</ul>
	</div>
	<% end %>
	<p>
		<%= f.label :japanese %><br>
		<%= f.text_area :japanese %>
	</p>

	<p>
		<%= f.label :english %><br>
		<%= f.text_area :english %>
	</p>

	<p>
		<%= f.submit %>
	</p>
<% end %>
